import {portfolio} from '@/data/portfolio';
import {notFound} from 'next/navigation';
import Link from 'next/link';
import {Reveal,ProjectVideo} from '@/components/StudioMotion';
export function generateStaticParams(){return portfolio.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=portfolio.find(p=>p.slug===slug);return {title:p?.title??'Project not found',description:p?.tagline}}
export default async function Project({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const index=portfolio.findIndex(p=>p.slug===slug);if(index<0)notFound();const p=portfolio[index],next=portfolio[(index+1)%portfolio.length];return <main id="main"><section className="case-hero"><Link className="case-back" href="/work">↖ Back to all work</Link><div className="case-topline"><h1>{p.title}</h1><span>{p.category} / Selected work</span></div><p>{p.tagline}</p></section><div className="case-cover"><img src={p.image} alt={`${p.title} brand artwork`} width="1600" height="900" fetchPriority="high"/></div><section className="case-description"><div><span className="eyebrow">WHAT WE DID</span><ul>{p.services.map(s=><li key={s}>{s}</li>)}</ul></div><Reveal><p>{p.description}</p></Reveal></section><section className="case-gallery" aria-label={`${p.title} project gallery`}>{p.video&&<Reveal><ProjectVideo src={`/assets/portfolio/${p.video}.mp4`} poster={`/assets/portfolio/${p.video}.webp`}/></Reveal>}{p.gallery?.map((src,i)=><Reveal key={src}><img src={src} alt={`${p.title} — design detail ${i+1}`} loading="lazy"/></Reveal>)}{p.links&&<div className="case-films">{p.links.map(l=><a key={l.url} href={l.url} target="_blank" rel="noreferrer"><img src={l.image} alt={l.label} loading="lazy"/><span>{l.label}<b>Watch film ↗</b></span></a>)}</div>}</section><section className="case-next"><span className="eyebrow">UP NEXT / KEEP EXPLORING</span><Link href={`/work/${next.slug}`}>{next.title} ↗</Link></section></main>}
